#include "main.h"

/**
 * print_alphabet -  prints ten times the alphabet, in lowercase
 */

void print_alphabet ten times(void)
{
	char line, ch;

	for (line = 0; line <= 9; line++)
	{
		for (ch = "a"; ch <= "z"; ch++)
	
			_putchar(ch);
		_putchar('\n');

	}
}
